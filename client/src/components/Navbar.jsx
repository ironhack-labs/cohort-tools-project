import { Link } from "react-router-dom";

function Navbar() {
  return (
    <nav>
    <h1>Cohort Tools</h1>
      {/* <Link to="/">
        <button>Home</button>
      </Link>

      <Link to="/cohorts">
        <button>Projects</button>
      </Link> */}
    </nav>
  );
}

export default Navbar;
